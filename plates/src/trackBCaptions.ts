import type {Caption} from "./types";

/**
 * Captions for the four live plates.
 * Same clock as mapTrackBCaptions(plates/fixtures/track-b-words.json).
 */
export const TRACK_B_CAPTIONS: Caption[] = [
  {text: " Keep", startMs: 120, endMs: 480},
  {text: " moving", startMs: 500, endMs: 920},
  {text: " today.", startMs: 1020, endMs: 1400},
];
