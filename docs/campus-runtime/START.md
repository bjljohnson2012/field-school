# Start here

Waves 1–3 and plates Wave 5 are PASS. Do not resume Wave 2.

Cursor / Field School PM, send only this:

```
Read docs/LAW.md. Then docs/staff/COMPANY_GRAPH.md. Then docs/staff/BUILD_GRAPH.md.
Inspect the repo. Print C1-C7 and N1-N15. Spawn at most two whose files do not collide.
Default pair: one company stream (docs/) + one Dev stream (app/). Do not open a cursor/wave-N branch. Do not default to Wave 2.
```

Law file lists the rest. Campus-runtime WAVE*.md files are proof, not tickets.
