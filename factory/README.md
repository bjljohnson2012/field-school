# Factory quality gate

Operator-only Cap → Remotion → Edit-spec path. No campus UI.

After ingest/STT, Video writes an Edit spec at Review. The checklist job validates the six locked items. Pass all six → Status becomes Cleaning. Any fail → hold at Review and escalate to the Chief Decision Maker. Publish and Distributed stay held. Asset Just (`27pn9xs0zk8a73g`) cannot leave lock.

Live drop-in lives under `factory/vps-dropin/`. Copy those files onto `/opt/fieldschool-adapter` on `2.24.70.248` only. Do not copy them to CNC vault `2.24.64.248`.
