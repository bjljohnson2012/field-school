# Field School Remotion course videos

Cap is capture. Remotion is editorial. Student deliverable is 16:9 lesson master. CapCut shorts later via `cuts.json`.

The Remotion project copy of this file is `vault/fieldschool-edit/remotion/AGENTS.md`. That file is law. Paste it as the first message of every new episode chat.

WhisperX align is the clock. Enhance VO first. Words appear at `fromMs`. Active word gold `#C4A35A`. Keyword cues snap Vox + SFX. Silence 400ms = cut. 800ms = Vox enter.

Install: `npx skills add remotion-dev/skills`. Pin Remotion packages exact to one version. Current pin `4.0.518`: `remotion` `@remotion/cli` `@remotion/media` `@remotion/captions` `@remotion/rounded-text-box` `@remotion/layout-utils` `@remotion/google-fonts` `@remotion/transitions` `@remotion/shapes` `@remotion/paths` `@remotion/effects`.

CPU whisperx: `--model small --device cpu --compute_type int8`
Render: `--concurrency=1 --frames=0-150` first.

Brand: `#11140C` `#EFE7D6` `#1A1A16` `#C4A35A` `#6B7F4F`. Fraunces / Source Serif 4 / IBM Plex Sans. Do not overwrite the wheat FieldSchoolClip look-dev.

See `CURSOR_FOLLOWUP.md` for the punch list.
