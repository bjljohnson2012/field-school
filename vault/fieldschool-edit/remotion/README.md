# Field School Remotion lesson

Vox-style cut for Cap takes. Lives on the Field School VPS at `/opt/fieldschool-edit/remotion`. Do not deploy this to CNC vault.

Talking head stays in a right-hand card. Type lives in the left column. Words come from a WhisperX-shaped `words.json` (`text`, `start`, `end`). Intro uses the signed wheat kit from `fieldschool.ai/branding`.

You Can Just Do Things preview and final publish under `https://cap.fieldschool.ai/hls/j013r823wx9ecaf/vox/`.
