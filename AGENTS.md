# Agents working in this repo

Read `docs/LAW.md`. Then `docs/staff/LOOP.md`. Then `docs/staff/state.json`.
Run `node docs/staff/pick-next.mjs`. Do that pick. Launch stays CLOSED 0/8.

The ethos exists so finance, ICP, research, marketing, GTM, ops, and Dev stay one company.
The loop exists so work continues at 06 / 09 / 15 / 22 / 02 ET without a chat staying open.
Waves 1–3 and plates Wave 5 are PASS. Do not resume Wave 2. Do not open `cursor/wave-N-*`.

Job and locks: `FIELD-SCHOOL-ETHOS-MEMO.md`.
Chrome: `docs/staff/SHELLS.md`.
Factory: `docs/staff/FACTORY_VIDEO.md`.
Grok Bot: `docs/staff/GROK_BOT.md`.
How PM runs: `docs/staff/FIELD_SCHOOL_PM_LOOP.md`.
Staff wire: `docs/staff/GRAPH.md` (roster only).

Two rooms: household and sales. Together. Household is not the default. One ticket, one room.
LOOP.md wins on how to pick. COMPANY_GRAPH wins on which function. BUILD_GRAPH wins on which Dev node.
Ethos wins on Child vs User, prices, AUTH_URL, dest. `state.json` wins on PASS this hour.

## Do

- Build in `app/`. Company artefacts in `docs/icp-*.md`, `docs/finance-units.md`, `docs/gtm-hire.md`.
- Factory in `plates/` only. WhisperX → `@remotion/captions`. Four plates.
- Scope by org_id / membership_id. Keep guest Grok Bot. Quiz items need source_unit_id.
- Label login learners vs tracked children. Never mix them on one desk.
- Patch `docs/staff/state.json` on the same PR as the node.

## Do not

- Extend frozen TanStack `src/`.
- Touch CNC vault 2.24.64.248.
- Flip AUTH_URL. Re-render Just. Steal `bc-4765f2f0`.
- Official psychometric banks. Remotion-in-Next. Django. Pages in FastAPI. Qdrant this week.
- Child login. Salesperson as a child. Fourth SKU. Live card. Public flip. Market count.
- Default to household. Put both rooms in one ticket.
- Treat dest SHA, hire-path tests, or chrome PASS as the company end.
